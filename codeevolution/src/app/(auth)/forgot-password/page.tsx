import React from 'react'

function page() { // when we don't need route but we need to all thing in a folder then we use (folder name) so it will not take as route and it go direct to their child like this folder in auth folder but i give () to auth folder so we can use it direct like localhost:3000/forgot-password
  return (
    <div>
      Forgot-Password
    </div>
  )
}

export default page
